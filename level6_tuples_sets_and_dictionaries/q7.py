# Q7. Remove duplicates from a list using a set.

numbers = [1, 2, 2, 3, 4, 4, 5, 5]

unique_numbers = list(set(numbers))

print("Original list:", numbers)
print("After removing duplicates:", unique_numbers)