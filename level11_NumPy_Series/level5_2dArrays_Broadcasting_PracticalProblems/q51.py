# Q51 - Find the cumulative sum of all elements in a NumPy array.

import numpy as np

arr = np.array([1, 2, 3, 4, 5])

cumulative_sum = np.cumsum(arr)

print("Original array:", arr)
print("Cumulative sum:", cumulative_sum)