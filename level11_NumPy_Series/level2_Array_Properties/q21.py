# Q21. Find the sum and average of all elements in a NumPy array.

import numpy as np

arr = np.array([10, 20, 30, 40, 50])

total = np.sum(arr)
average = np.mean(arr)

print("Array:", arr)
print("Sum:", total)
print("Average:", average)