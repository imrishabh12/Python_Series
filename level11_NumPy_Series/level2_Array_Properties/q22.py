# Q22. Find the maximum and minimum elements in a NumPy array.

import numpy as np

arr = np.array([25, 10, 45, 5, 30])

maximum = np.max(arr)
minimum = np.min(arr)

print("Array:", arr)
print("Maximum:", maximum)
print("Minimum:", minimum)